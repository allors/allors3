import { workspaceMetaTests } from './workspace-meta-tests';

describe('workspaceMetaTests', () => {
  it('should work', () => {
    expect(workspaceMetaTests()).toEqual('workspace-meta-tests');
  });
});
