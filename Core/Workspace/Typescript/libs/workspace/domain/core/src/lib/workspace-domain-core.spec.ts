import { workspaceDomainCore } from './workspace-domain-core';

describe('workspaceDomainCore', () => {
  it('should work', () => {
    expect(workspaceDomainCore()).toEqual('workspace-domain-core');
  });
});
