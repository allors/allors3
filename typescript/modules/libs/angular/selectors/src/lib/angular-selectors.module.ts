import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { SelectorsService } from './selectors.service';

@NgModule({
  imports: [CommonModule],
  providers: [SelectorsService],
})
export class AngularSelectorsModule {}
