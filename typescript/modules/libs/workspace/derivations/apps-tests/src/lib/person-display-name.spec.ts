import { workspaceDerivationsAppsTests } from './workspace-derivations-apps-tests';

describe('workspaceDerivationsAppsTests', () => {
  it('should work', () => {
    expect(workspaceDerivationsAppsTests()).toEqual('workspace-derivations-apps-tests');
  });
});
