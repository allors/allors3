import { workspaceDerivationsAppsCustom } from './workspace-derivations-apps-custom';

describe('workspaceDerivationsAppsCustom', () => {
  it('should work', () => {
    expect(workspaceDerivationsAppsCustom()).toEqual('workspace-derivations-apps-custom');
  });
});
