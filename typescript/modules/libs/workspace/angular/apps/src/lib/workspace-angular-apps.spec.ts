import { workspaceAngularApps } from './workspace-angular-apps';

describe('workspaceAngularApps', () => {
  it('should work', () => {
    expect(workspaceAngularApps()).toEqual('workspace-angular-apps');
  });
});
